import { ShowallridebtnDirective } from './showallridebtn.directive';

describe('ShowallridebtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowallridebtnDirective();
    expect(directive).toBeTruthy();
  });
});
